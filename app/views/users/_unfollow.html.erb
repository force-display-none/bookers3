<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete, "data-confirm" => "フォローを外しますか？" }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-default" %>
<% end %>