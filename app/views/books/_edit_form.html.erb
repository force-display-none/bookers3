<h2>Editing Book</h2>

<%= form_for(book) do |f| %>
	<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>Opinion</h4>
	<%= f.text_area :body %>

<div>
    <%= f.submit 'Update Book' %><br>
</div>
<% end %>
<%= link_to "Back", book_path(@book) %> | <%= link_to "Books", books_path %>
