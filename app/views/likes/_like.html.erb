<%= form_for(@book_show.likes.build, html: { id: 'likes'}, remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, @book_show.id %></div>
  <%= button_tag(class: "btn btn-default btn-xs") do %>
    <%= content_tag :span, "#{@book_show.likes_count}", class: "glyphicon glyphicon-heart-empty" %>
  <% end %>
<% end %>